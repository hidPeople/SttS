# AGENT.md

このファイルは、今後このプロジェクトを開発するエージェントが常に参照する開発指針です。
以降の開発で継続的に守るべき指示が増えた場合は、このファイルに追記してください。

## Project

- このプロジェクトは、Slay the Spire系のデッキ構築型ローグライク戦闘画面を Phaser で検証するプロトタイプです。
- 目的は、カード、山札、手札、捨て札、敵行動、バフ・デバフ、レリック、HP/EP/Block/Energyなどの基本ループを実装・検証することです。
- 見た目は現時点では図形、テキスト、簡易エフェクト中心で構いません。本番用アセットは後から導入します。
- 将来的に Tauri でデスクトップアプリ化する予定があります。Web前提の実装に偏りすぎず、ローカルアプリ化しやすい構造を維持してください。

## Tech Stack

- Phaser 3
- TypeScript
- Vite
- 将来: Tauri

画面サイズは現在 `1280x720` を前提にしています。

## Commands

- 依存関係のインストール: `npm install`
- 開発サーバー起動: `npm run dev`
- ビルド確認: `npm run build`

実装後は原則 `npm run build` を実行して TypeScript と Vite のビルドが通ることを確認してください。

## Architectural Rules

- `BattleScene` にすべてをベタ書きしすぎないでください。戦闘の表示・演出は `BattleScene` が担当して構いませんが、データ定義と状態管理は分離してください。
- カード、敵、プレイヤー、レリック、バフ・デバフは、できるだけ `src/data` と `src/models` 側に分離します。
- 新しいカード、敵、レリック、バフ・デバフは、可能な限りデータ追加・変更だけで挙動を制御できる設計を優先してください。
- カードやレリックの効果タイミングは、`timing` によるフック方式で拡張できるようにしてください。
- フック処理では、所持レリックや状態異常を毎回個別の `if` で増やすのではなく、タイミング別の配列・Mapなどで処理対象を引ける構造を維持してください。
- 過剰設計は避けつつ、今後の追加が明らかな箇所はデータ駆動に寄せてください。

## Gameplay Terms

- HPはダメージで減少し、0になると倒れます。
- EPはダメージで増加し、Maxに到達すると Peak 処理が発生します。
- プレイヤーEP Peak時は Lingering などの状態処理が発生します。
- 敵EP Peak時のHPドレインは、レリックなどのフック効果として扱います。
- HP攻撃、EP攻撃、Block、Drain、Healなどは、演出と数値表示をできるだけ一致させてください。
- バフ・デバフはスタック可能です。通常は1ターンに1つ消費しますが、例外仕様を持つ状態はその状態の定義・フックで扱います。

## Data Definitions

- カード定義は全カードで同じ要素を持たせ、不要な値は `0` や空配列にしてください。
- カードには、HP/EPダメージ、攻撃回数、自傷、回復、ドロー、エナジー回復、Block、バフ、デバフ、廃棄、レア度などを定義できるようにしてください。
- カードの `categories` は1つ目を色つきカテゴリにしてください。`noMotion` は拘束中でも使えることを示す補助カテゴリで、単独または先頭カテゴリにはしないでください。
- 敵定義は通常行動 `intents` と、Charm時などの特殊行動 `intents_E` を分けます。
- 拘束中などE行動とは別系統の特殊行動は `intents_B` と `intentBConditions` に分け、条件成立時は `intents_E` より優先します。
- `intents_E` が空の敵には Charm が効かず、MISS演出を出します。
- レリック定義はカードと同じように `src/data` に分離し、`timing` で発動タイミングを制御します。
- レア度はカード・レリックともに報酬画面で使う想定です。スターター、コモン、アンコモン、レア、イベントを扱います。

## UI And Interaction Rules

- 手札は最大10枚です。11枚目以降を引いた場合は捨て札へ送ります。
- ドロー、追加、捨て札、廃棄などの演出中カードにはホバー・クリックを発火させないでください。
- ドロー演出中は、全カードのドロー完了まで手札全体の入力をロックしてください。
- End Turnは、プレイヤーターン中かつドローやターン開始処理が完了している時だけ押せます。無効中はグレーアウトしてください。
- 基本状態の手札の重なり順は、右側のカードが左側のカードより前面に来るようにしてください。

## Visual And Asset Rules

- まずは Rectangle、Text、Circle、Graphics などの仮素材で構いません。
- 後から画像素材に差し替えやすい構造にしてください。
- キャラクターの状態表示は、状態ごとに全身フレームを複製する方針にしないでください。
- 状態異常や特殊状態の見た目は、将来的に透過素材差分をキャラクターに重ねるレイヤー化オーバーレイ方式で表現する想定です。
- HP回復時は、緑のプラスマーク演出と緑色の回復数値を表示します。
- ダメージ・回復など、数値が発生する効果は画面上の演出と数値表示を対応させてください。

## Text And Tooltip Rules

- バフ・デバフはテキスト一覧ではなく、アイコンのみで表示します。
- アイコンホバー時は説明ツールチップを表示します。ステータスが消えた時は、表示中のツールチップも閉じてください。
- カード上に出る用語説明ツールチップは、関連するバフ・デバフの説明を表示します。カード上の用語説明にはスタック数は不要です。
- カードや敵行動の数値がバフ・デバフ・レリックで変動している場合、全文ではなく変動した数字部分だけを太字にしてください。

## Git Rules

- コミットする場合、コミットメッセージは日本語で記載してください。
- ユーザーが明示しない限り、既存の未コミット変更を勝手に戻さないでください。

## Documentation Rules

### Data Documentation Notes

- 状態異常を変更した場合は、`src/data/statuses.ts` の `triggers`, `effects`, `modifiers`, `visuals`, `allowedOwners` と `docs/battle-data-design-ja.md` の説明を必ず同期してください。
- カードカテゴリや敵行動プールを変更した場合も、`docs/battle-data-design-ja.md` と同期してください。特に色を持たない補助カテゴリは型制約と説明を一致させてください。
- 状態異常の演出はデータ側に処理を書かず、`visuals` に演出キーを置き、実体はSceneまたは演出実行モジュール側に置いてください。
- ToDoを完了した場合は、`docs/data-driven-todo-ja.md` から該当項目を外し、完了内容と残した専用処理を `docs/data-driven-done-archive-ja.md` に追記してください。

- 戦闘データ構造、カード、敵、レリック、状態、報酬、フックtimingを変更した場合は、必ず `docs/battle-data-design-ja.md` を更新してください。
- データ駆動化方針や未完了課題を変更した場合は、必ず `docs/data-driven-todo-ja.md` を更新してください。
- `docs/data-driven-todo-ja.md` の項目を完了した場合は、削除せず `docs/data-driven-done-archive-ja.md` へ移してください。後から経緯を確認できるよう、完了日と対応内容を短く残してください。
- 外部データ編集ツールを将来作る前提で、設計書には「UIで何を入力すれば何が起きるか」が分かる説明を残してください。
- 実装と設計書が食い違う状態を残さないでください。プロトタイプ都合で未実装の理想仕様を書く場合は、設計書ではなくToDoに書いてください。
